package:
  name: mdplifs
  version: "0.0.0"

source:
  path: ../..

build:
  number: 0

requirements:
  host:
    - python
    - setuptools

  run:
    - python
    - numpy
    - scipy
    - mdtraj

test:
  requires:
    - pytest
  imports:
    - mdplifs

about:
  home: add_url_here
  license: MIT License
